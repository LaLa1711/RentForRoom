@model RentForRoom.DBContext.tbAlbum

@{
    ViewBag.Title = "Details";
    Layout = "~/Areas/Admin/Views/Shared/_Layout_Admin.cshtml";
}

<main id="main" class="main">

    <div class="pagetitle">
        <h1>QUẢN LÝ THÔNG TIN ALBUM</h1>
    </div><!-- End Page Title -->

    <section class="section">
        <div class="row">
            <div class="col-lg-12">

                <div class="card">
                    <div class="card-body">
                        <h5 class="card-title">THÔNG TIN ALBUM</h5>

                        <form action="/Admin/AAlbums/Edit/@Model.IDAlbum" method="post" enctype="multipart/form-data">

                            @Html.HiddenFor(model => model.IDAlbum)
                            <div class="row mb-3">
                                <label for="inputNumber" class="col-sm-2 col-form-label">Hình Ảnh</label>
                                <div class="col-sm-10">
                                    @if (string.IsNullOrEmpty(Model.HinhAnh))
                                    {
                                        Model.HinhAnh = "/Content/img/avatarfb.jpg";
                                        <img src="@Model.HinhAnh" width="180" />

                                    }
                                    else
                                    {
                                        <img src="@Model.HinhAnh" width="180" />}
                                </div>
                            </div>
                            <div class="row mb-3">
                                <label for="inputText" class="col-sm-2 col-form-label">ID Phòng</label>
                                <div class="col-sm-10">
                                    <div class="form-control">
                                        @Html.Raw(Model.IDPhong)
                                    </div>
                                </div>
                            </div>
                            <div class="row mb-3">
                                <label for="inputText" class="col-sm-2 col-form-label">Tiêu Đề</label>
                                <div class="col-sm-10">
                                    <div class="form-control">
                                        @Html.Raw(Model.TieuDe)
                                    </div>
                                </div>
                            </div>
                            <div class="row mb-3">
                                <label for="inputText" class="col-sm-2 col-form-label">Hide</label>
                                <div class="col-sm-2">
                                    <div class="form-control text-center">
                                        <strong>

                                            @if (Model.Hide != true)
                                            {
                                                <span>Còn Hoạt Động</span>
                                            }
                                            else
                                            {
                                                <span>Tạm Ngưng</span>
                                            }
                                        </strong>
                                    </div>
                                </div>
                            </div>
                            <div class="row mb-3">
                                <div class="col-sm-10">
                                    <a href="/Admin/AAlbums" class="btn btn-danger"> Quay Lại</a>
                                </div>
                            </div>

                        </form><!-- End General Form Elements -->
                    </div>
                </div>
            </div>

        </div>
    </section>

</main>
