@model IEnumerable<RentForRoom.DBContext.tbTinhThanhPho>

@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_Layout_Admin.cshtml";
}

<main id="main" class="main">

    <div class="pagetitle">
        <h1>QUẢN LÝ THÔNG TIN THÀNH PHỐ</h1>
    </div><!-- End Page Title -->

    <section class="section">
        <div class="row">
            <div class="col-lg-12">
                <div class="card">
                    <div class="card-body">
                        <h5 class="card-title">DANH SÁCH CÁC THÔNG TIN THÀNH PHỐ</h5>
                        <a class="btn btn-success" href="/Admin/ATinhThanhPhoes/Create">THÊM MỚI</a>
                    </div>

                    <table class="table">
                        <thead>
                            <tr>
                                <th>
                                    Thành Phố
                                </th>
                                <th>
                                    Trạng Thái
                                </th>
                            </tr>
                        </thead>

                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>
                                    @Html.DisplayFor(modelItem => item.TenTP)
                                </td>
                                <td class="custom-padding">
                                    @if (item.Hide != true)
                                    {
                                        <p>         Hiện</p>
                                    }
                                    else
                                    {
                                        <p>   ẨN </p>
                                    }
                                </td>
                                <td>
                                    <a href="/Admin/ATinhThanhPhoes/Edit/@item.IDTP"><i class="bi bi-pencil-square"></i></a> |
                                    <a href="/Admin/ATinhThanhPhoes/Details/@item.IDTP"><i class="ri-article-line"></i></a> |
                                    <a href="/Admin/ATinhThanhPhoes/Delete/@item.IDTP"><i class="ri-delete-bin-6-line"></i></a>
                                </td>
                            </tr>
                        }

                    </table>

                </div>


            </div>
        </div>
    </section>

</main>
